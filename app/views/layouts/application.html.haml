!!!
%html
  %head
    %title=content_for?(:title) ? yield(:title) : '声優の携帯電話'
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    %h1=link_to '声優の携帯電話', '/'
    = yield
